# ---------------------------------------------------------------------------
# Phase classifications
# ---------------------------------------------------------------------------
AGENT_ONLY_PHASES = ["explore", "plan"]
AGENT_PLUS_WRITE_PHASES = ["review"]
CODING_PHASES = ["write-tests", "write-code", "validate", "code-review", "report"]
PLAN_ALLOWED_STOP_PHASES = ["present-plan", "completed", "failed"]

# ---------------------------------------------------------------------------
# Agent invocation limits
# ---------------------------------------------------------------------------
EXPLORE_MAX = 3
RESEARCH_MAX = 2
PLAN_MAX = 1
PLAN_REVIEW_MAX = 3
TEST_REVIEWER_MAX = 3
QA_MAX = 1
CODE_REVIEW_MAX = 3


CODEBASE_MD = "CODEBASE.md"

# ---------------------------------------------------------------------------
# Safe domains for web access
# ---------------------------------------------------------------------------
SAFE_DOMAINS = [
    "docs.python.org",
    "docs.anthropic.com",
    "developer.mozilla.org",
    "reactjs.org",
    "react.dev",
    "nextjs.org",
    "tailwindcss.com",
    "github.com",
    "stackoverflow.com",
    "pypi.org",
    "npmjs.com",
    "typescriptlang.org",
    "nodejs.org",
    "firebase.google.com",
    "supabase.com",
    "expo.dev",
    "reactnative.dev",
    "code.claude.com",
    "vercel.com",
    "medium.com",
    "web.dev",
    "developers.google.com",
    "css-tricks.com",
]


# ---------------------------------------------------------------------------
# Phase types
# ---------------------------------------------------------------------------
PHASES = [
    "explore",
    "plan",
    "write-plan",
    "review",
    "write-tests",
    "write-code",
    "report",
]



# ---------------------------------------------------------------------------
# Agent types
# ---------------------------------------------------------------------------
AGENT_TYPES = [
    "Explore",
    "Research",
    "QualityAssurance",
    "TestReviewer",
    "CodeReviewer",
    "PlanReview",
    "Plan",
]

# ---------------------------------------------------------------------------
# Required agents per phase
# ---------------------------------------------------------------------------
[REQUIRED_AGENTS]
explore = ["Explore", "Research"]
plan = ["Plan"]
plan-review = ["PlanReview"]
write-tests = ["TestReviewer"]
validate = ["QualityAssurance"]
code-review = ["CodeReviewer"]
write-report = ["Report"]

# ---------------------------------------------------------------------------
# Score thresholds
# ---------------------------------------------------------------------------
[SCORE_THRESHOLDS.plan]
confidence_score = 90
quality = 90

[SCORE_THRESHOLDS.tests]
confidence_score = 90
quality = 90

[SCORE_THRESHOLDS.code]
confidence_score = 90
quality = 90
# ---------------------------------------------------------------------------
# File Paths
# ---------------------------------------------------------------------------


DEFAULT_STATE_JSONL_ =  ".claude/state.jsonl"
LOG_FILE = BUILD_ROOT / "build.log"
DEBUG_LOG_FILE = DEBUG.log



PLAN_FILE_PATH = ".claude/plans/plan.md"
TEST_FILE_PATH = ".claude/tests/test.md"
CODE_FILE_PATH = ".claude/code/code.md"
REPORT_FILE_PATH = ".claude/reports/report.md"


# ---------------------------------------------------------------------------
# File Paths
# ---------------------------------------------------------------------------

[PHASE_REQUIREMENTS.]