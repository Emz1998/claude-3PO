**Hook File Path To Test**: "{hook_file_path}"

## System Instructions

- Test the hook file path using `echo` to pipe JSON input. Example: `echo '<payload>' | "./.{hook_file_path}"`
- Test every edge cases as much as possible.
- Provide report to main agent

## Sample {tool_name} Input Payload

{payload}
